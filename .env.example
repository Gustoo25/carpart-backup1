# Stripe API keys — get these from https://dashboard.stripe.com/apikeys
# Use TEST keys (sk_test_..., pk_test_...) until you're ready to take real payments.
STRIPE_SECRET_KEY=sk_test_replace_me
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me

# Stripe webhook signing secret.
# Local dev: run `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
#   — the CLI prints a whsec_... value, paste it here.
# Production: get it from https://dashboard.stripe.com/webhooks after creating
#   an endpoint pointing at https://yourdomain.com/api/webhooks/stripe.
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Postgres connection string from Neon — https://console.neon.tech
# Format: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Sentry error tracking — https://sentry.io
# Create a project (Next.js), then paste the DSN below.
# Without these, Sentry is a complete no-op (safe to leave blank in dev).
# SENTRY_ORG / SENTRY_PROJECT / SENTRY_AUTH_TOKEN are only needed for
# source map upload during production builds.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Upstash Redis — used for rate-limiting /api/checkout (10 req/min per IP).
# Sign up at https://console.upstash.com → create a Redis DB → copy REST URL + token.
# Dev: leaving these blank disables rate limiting with a one-time warning.
# Prod: missing values cause /api/checkout to return 503 (fail closed).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Public URL of your site (used for Stripe success/cancel redirects)
# In dev: http://localhost:3000
# In prod: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000
